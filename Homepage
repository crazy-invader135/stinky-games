<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Slop Games and Junk</title>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
  <style>
    /* Global Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Orbitron', sans-serif;
    }

    body {
      background: radial-gradient(circle at top left, #0b0b0d, #1a1a25);
      color: #00f0ff;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
      transition: opacity 0.5s ease;
    }

    header {
      width: 100%;
      text-align: center;
      padding: 20px 0;
      animation: glow 2s infinite alternate;
    }

    header h1 {
      font-size: 4rem;
      color: #0ff;
      text-shadow: 0 0 5px #0ff, 0 0 10px #0ff, 0 0 20px #00f0ff, 0 0 40px #00f0ff;
    }

    @keyframes glow {
      0% { text-shadow: 0 0 5px #0ff, 0 0 10px #0ff, 0 0 20px #00f0ff, 0 0 40px #00f0ff; }
      50% { text-shadow: 0 0 10px #0ff, 0 0 20px #0ff, 0 0 30px #00f0ff, 0 0 60px #00f0ff; }
      100% { text-shadow: 0 0 5px #0ff, 0 0 10px #0ff, 0 0 20px #00f0ff, 0 0 40px #00f0ff; }
    }

    main {
      margin-top: 40px;
      width: 100%;
      max-width: 800px;
      color: #00f0ffcc;
    }

    main h2 {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #0ff;
      text-shadow: 0 0 5px #0ff;
    }

    .competition {
      margin-bottom: 30px;
    }

    .ai-entry {
      margin: 10px 0;
      font-size: 1.1rem;
    }

    a {
      color: #0ff;
      text-decoration: underline;
      cursor: pointer;
    }

    a:hover {
      color: #00f0ff;
      text-shadow: 0 0 5px #0ff;
    }
  </style>
</head>
<body>

  <header>
    <h1>AI Slop Games and Junk</h1>
  </header>

  <main>
    <div class="competition">
      <h2>Competition 1: Physics Simulator</h2>
      
      <div class="ai-entry">
        AI 1: <a href="https://sites.google.com/view/ai-slop/chatgpt-phisics" class="redirect">HERE</a>  
        &nbsp;&nbsp;Total mark: /20  
        &nbsp;&nbsp;Looks: /10  
        &nbsp;&nbsp;Functionality: /10
      </div>
      
      <div class="ai-entry">
        AI 2: <a href="https://sites.google.com/view/ai-slop/grok-phisics" class="redirect">HERE</a>  
        &nbsp;&nbsp;Total mark: /20  
        &nbsp;&nbsp;Looks: /10  
        &nbsp;&nbsp;Functionality: /10
      </div>
    </div>
  </main>

  <script>
    // Fade-out redirect effect
    document.querySelectorAll('a.redirect').forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault(); // stop default jump
        const url = this.href;
        document.body.style.opacity = 0; // fade out
        setTimeout(() => {
          window.location.href = url; // redirect after fade
        }, 500); // matches CSS transition duration
      });
    });
  </script>

</body>
</html>
